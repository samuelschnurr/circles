<MudNavMenu>
    @foreach (var item in navMenuItems)
    {
        <span class="@(item.activeOnRoot && IsRootPath() ? "activeOnRoot" : "")">
            <MudNavLink Href="@item.href" Icon="@item.icon" Match="item.match">@item.label</MudNavLink>
        </span>
    }
</MudNavMenu>