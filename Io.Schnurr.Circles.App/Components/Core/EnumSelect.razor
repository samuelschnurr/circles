@typeparam T where T : Enum

<MudPaper Elevation="0" MinWidth="@(MinWidth.ToString() + "px")">
    <MudSelect Value="SelectedValue" ValueChanged="OnSelectedValueChanged"
               Class="@Class" Label="@Label" AnchorOrigin="Origin.BottomCenter"
               For="For" Immediate="Immediate">
        @foreach (T item in Enum.GetValues(typeof(T)))
        {
            <MudSelectItem Value="@item">@item.GetDisplayName()</MudSelectItem>
        }
    </MudSelect>
</MudPaper>